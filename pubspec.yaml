name: circular_loading_button
description: circular_loading_button is simple Flutter loading/progress animated button.
version: 1.0.4
homepage: https://github.com/elchief84/loading_button

environment:
  sdk: '>=3.1.2 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
